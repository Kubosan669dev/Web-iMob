import UiKitPage from "./UiKitPage.jsx";

// HomePage: sẽ được ghép từ các section (Hero, About, Services...) ở Bước 3-6.
// Bước 1: tạm hiển thị UI kit để duyệt design system.
export default function HomePage() {
  return <UiKitPage />;
}
